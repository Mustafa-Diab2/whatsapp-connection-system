[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build --workspace=@repo/api"

[deploy]
startCommand = "npm start --workspace=@repo/api"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
